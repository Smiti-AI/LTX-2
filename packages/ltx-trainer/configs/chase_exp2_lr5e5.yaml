# =============================================================================
# CHASE_PP LoRA — EXP-2: LR 5e-5 fix, rank 32 + FFN, 960×832, 10k steps
# =============================================================================
#
# EXP-1 post-mortem: loss diverged at step ~2800 back to 0.95+ after reaching
#   0.53 low. Root cause: LR 1e-4 too high for 960×832 resolution (53% more
#   tokens than SKYE_PP 960×544, larger gradient magnitude).
#
# Single change from EXP-1: learning_rate 1e-4 → 5e-5.
# Everything else identical so this is a clean single-axis test.
#
# Checkpoint interval: 2000 steps (5 checkpoints total) — 500 was wasteful.
# Validation interval: 2000 steps to match.
#
# Run on H100 (34.56.137.11):
#   cd /home/efrat_t_smiti_ai/projects/LTX-2
#   nohup env PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True \
#     /usr/local/bin/uv run python packages/ltx-trainer/scripts/train.py \
#     packages/ltx-trainer/configs/chase_exp2_lr5e5.yaml \
#     > /tmp/ltx_current.log 2>&1 &
# =============================================================================

model:
  model_path: "/home/efrat_t_smiti_ai/models/LTX-2.3/ltx-2.3-22b-dev.safetensors"
  text_encoder_path: "/home/efrat_t_smiti_ai/models/gemma-3-12b-it-qat-q4_0-unquantized"
  training_mode: "lora"
  load_checkpoint: null

lora:
  rank: 32
  alpha: 32
  dropout: 0.0
  target_modules:
    - "to_k"
    - "to_q"
    - "to_v"
    - "to_out.0"
    - "ff.net.0.proj"
    - "ff.net.2"
    - "audio_ff.net.0.proj"
    - "audio_ff.net.2"

training_strategy:
  name: "text_to_video"
  first_frame_conditioning_p: 0.5
  with_audio: true
  audio_latents_dir: "audio_latents"

optimization:
  learning_rate: 5e-5          # EXP-1 used 1e-4 — too high for 960×832, diverged at step ~2800
  steps: 10000
  batch_size: 1
  gradient_accumulation_steps: 1
  max_grad_norm: 1.0
  optimizer_type: "adamw"
  scheduler_type: "linear"
  scheduler_params: {}
  enable_gradient_checkpointing: true

acceleration:
  mixed_precision_mode: "bf16"
  quantization: null
  load_text_encoder_in_8bit: true

data:
  preprocessed_data_root: "/home/efrat_t_smiti_ai/data/golden_chase_preprocessed"
  num_dataloader_workers: 2

validation:
  prompts:
    - "CHASE_PP, the German Shepherd pup from PAW Patrol, stands at attention in his blue police uniform and hat. He salutes sharply and says 'CHASE_PP is on the case!' His brown eyes are focused and alert, tail wagging with determination. The audio captures his confident, clear voice."
    - "CHASE_PP from PAW Patrol sits in his blue police cruiser, scanning the road ahead with alert eyes. He speaks into his badge communicator: 'Ryder, I'm on my way!' His ears are perked up and his expression is serious but eager. The audio has his voice and the sound of the engine."
    - "CHASE_PP stands at the Lookout beside SKYE_PP, his tail wagging as he looks up at Ryder. He says 'No job too big, no pup too small!' His blue uniform gleams, and his expression is proud and confident. The audio captures his enthusiastic bark and clear voice."
  images: null
  negative_prompt: "worst quality, inconsistent motion, blurry, jittery, distorted"
  video_dims: [960, 832, 49]
  frame_rate: 24.0
  seed: 42
  inference_steps: 30
  interval: 2000               # was 500 — no need to validate every 500 steps
  videos_per_prompt: 1
  guidance_scale: 4.0
  stg_scale: 1.0
  stg_blocks: [29]
  stg_mode: "stg_av"
  generate_audio: true
  skip_initial_validation: false

checkpoints:
  interval: 2000               # was 500 — 5 checkpoints for 10k steps is enough
  keep_last_n: 0
  precision: "bfloat16"

flow_matching:
  timestep_sampling_mode: "shifted_logit_normal"
  timestep_sampling_params: {}

hub:
  push_to_hub: false
  hub_model_id: null

wandb:
  enabled: true
  project: "ltx-2-chase-lora"
  entity: null
  tags: ["ltx2", "lora", "chase", "exp2-lr5e5", "960x832", "rank32"]
  log_validation_videos: true

seed: 42
output_dir: "outputs/chase_exp2_lr5e5"
