# =============================================================================
# Skye LoRA — EXP-4: Image-to-Video Focus (rank 32 / 2500 steps, high I2V)
# =============================================================================
#
# Purpose: Optimize for generating Skye animations from a reference start frame.
# Useful for production: provide a clean Skye illustration and animate it.
#
# Key difference from EXP-2: first_frame_conditioning_p = 0.8 (vs 0.5).
# 80% of training steps condition on a clean first frame, training the model
# to continue motion from a given starting pose.
#
# Use this alongside EXP-2 — run after EXP-2 if I2V generation quality matters.
# =============================================================================

model:
  model_path: "/home/efrattaig/models/LTX-2.3/ltx-2.3-22b-dev.safetensors"
  text_encoder_path: "/home/efrattaig/models/gemma-3-12b-it-qat-q4_0-unquantized"
  training_mode: "lora"
  load_checkpoint: null

lora:
  rank: 32
  alpha: 32
  dropout: 0.0
  target_modules:
    - "to_k"
    - "to_q"
    - "to_v"
    - "to_out.0"

training_strategy:
  name: "text_to_video"
  first_frame_conditioning_p: 0.8   # ← HIGH: 80% of steps use clean first frame
  with_audio: true
  audio_latents_dir: "audio_latents"

optimization:
  learning_rate: 1e-4
  steps: 2500
  batch_size: 1
  gradient_accumulation_steps: 1
  max_grad_norm: 1.0
  optimizer_type: "adamw"
  scheduler_type: "linear"
  scheduler_params: {}
  enable_gradient_checkpointing: true

acceleration:
  mixed_precision_mode: "bf16"
  quantization: null
  load_text_encoder_in_8bit: true

data:
  preprocessed_data_root: "/home/efrattaig/data/golden_skye_preprocessed"
  num_dataloader_workers: 2

validation:
  prompts:
    - "SKYE, the Cockapoo pup from PAW Patrol, sits in her pink helicopter cockpit with her aviator helmet on. She says 'PAW Patrol, on a roll!' Her big purple eyes light up with excitement as she pulls back on the controls. The audio captures her enthusiastic voice and the hum of the helicopter blades."
    - "SKYE from PAW Patrol does an aerial barrel roll with her helicopter backpack, pink fur gleaming in the sunlight, ears streaming behind her as she spins. She calls out 'This pup's gotta fly!' The audio has her joyful laugh and the whirring of her rotors."
    - "SKYE sits at the Lookout tower beside Chase, her tail wagging eagerly. She tilts her head and says 'Don't worry, I've got this!' Her purple eyes are wide and bright, full of confidence. The audio captures her clear, cheerful voice and ambient outdoor sounds."
  # For I2V validation, uncomment and provide start frames:
  # images:
  #   - "/home/efrattaig/LTX-2/inputs/BM_v1/benchmark_v1/skye_helicopter_birthday_gili/start_frame.png"
  #   - "/home/efrattaig/LTX-2/inputs/BM_v1/benchmark_v1/skye_helicopter_birthday_gili/start_frame.png"
  #   - "/home/efrattaig/LTX-2/inputs/scenes/skye_concerned_lookout/start_frame.png"
  images: null
  negative_prompt: "worst quality, inconsistent motion, blurry, jittery, distorted"
  video_dims: [960, 544, 49]
  frame_rate: 25.0
  seed: 42
  inference_steps: 30
  interval: 250
  videos_per_prompt: 1
  guidance_scale: 4.0
  stg_scale: 1.0
  stg_blocks: [29]
  stg_mode: "stg_av"
  generate_audio: true
  skip_initial_validation: false

checkpoints:
  interval: 250
  keep_last_n: 3
  precision: "bfloat16"

flow_matching:
  timestep_sampling_mode: "shifted_logit_normal"
  timestep_sampling_params: {}

hub:
  push_to_hub: false
  hub_model_id: null

wandb:
  enabled: false
  project: "ltx-2-skye-lora"
  entity: null
  tags: ["ltx2", "lora", "skye", "exp4-i2v"]
  log_validation_videos: true

seed: 42
output_dir: "outputs/skye_exp4_i2v"